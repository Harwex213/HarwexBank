<Window x:Class="HarwexBank.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarwexBank"
        mc:Ignorable="d"
        Title="HarwexBank" Height="700" Width="820"
        WindowStartupLocation="CenterScreen">
        
        <Window.Resources>
                
                <DataTemplate DataType="{x:Type local:AuthorizationViewModel}">
                        <local:AuthorizationView />
                </DataTemplate>
                
                <DataTemplate DataType="{x:Type local:MainViewModel}">
                        <local:MainView />
                </DataTemplate>
                
        </Window.Resources>
        
        <ContentControl Content="{Binding SelectedControlViewModel}"/>
</Window>
